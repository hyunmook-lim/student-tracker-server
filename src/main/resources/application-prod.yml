server:
  port: 8080

spring:
  datasource:
    # Environment variables will be injected by the hosting platform (Koyeb/Render)
    # Environment variables will be injected by the hosting platform (Koyeb/Render)
    # Credentials are expected to be embedded in the DB_URL for simplicity
    url: ${DB_URL}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    root: INFO
